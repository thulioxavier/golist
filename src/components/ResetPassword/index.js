import { Fragment } from "react";
import * as S from "./styles";
export const ResetPassword = () => {
    return (
        <Fragment>
            <S.ResetButton>
                <S.Label>Recuperar acesso</S.Label>
            </S.ResetButton>
        </Fragment>
    );
}